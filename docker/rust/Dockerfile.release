FROM rust:1.68-bullseye

RUN apt-get update && \
    apt-get -y install git && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/* && \
    rustup update

# ready to shuttle
RUN cargo install cargo-shuttle